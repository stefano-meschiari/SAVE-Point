/*
 * Reads in the map.yaml and transforms it into linear arrays.
 */

"use strict";


var AppMenuView = Backbone.View.extend({
    // Top-level container
    el: $("#app-menu"),

    initialize: function() {
        
    }
});
